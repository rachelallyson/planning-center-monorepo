# People API vertices

Output is now generated under **docs/public/vertices/people/** (combined docs site).

From repo root: `npm run docs:export-people-site` (writes to `docs/public/vertices/people/`).  
To view: run `npm run docs:dev` and open http://localhost:3333/vertices/people/
