# Configuration Reference

Complete reference for all configuration options, environment variables, and defaults.

## PcoClientConfig

Main configuration interface for `PcoClient`:

```typescript
interface PcoClientConfig {
  auth: PcoAuthConfig;           // REQUIRED
  baseURL?: string;
  timeout?: number;
  headers?: Record<string, string>;
  debug?: boolean | PcoDebugOptions;
}
```

## Authentication

### Personal Access Token

```typescript
{
  type: 'personal_access_token';
  personalAccessToken: string;  // REQUIRED: Client ID
  personalAccessTokenSecret?: string;  // Optional: Client Secret (or use env var)
}
```

**Environment Variables** (Alternative to config):
- `PCO_PERSONAL_ACCESS_TOKEN`: Your Client ID
- `PCO_PERSONAL_ACCESS_SECRET`: Your Client Secret

**Configuration Options**:

**Option 1: Environment Variables (Recommended for production)**
```typescript
// Set environment variables
process.env.PCO_PERSONAL_ACCESS_TOKEN = 'your_client_id';
process.env.PCO_PERSONAL_ACCESS_SECRET = 'your_client_secret';

// Config only needs client ID
const client = new PcoClient({
  auth: {
    type: 'personal_access_token',
    personalAccessToken: process.env.PCO_PERSONAL_ACCESS_TOKEN!
  }
});
```

**Option 2: Direct Config (Convenient for development)**
```typescript
const client = new PcoClient({
  auth: {
    type: 'personal_access_token',
    personalAccessToken: 'your_client_id',
    personalAccessTokenSecret: 'your_client_secret'
  }
});
```

**Setup Steps**:
1. Visit [Planning Center Developer Account](https://api.planningcenteronline.com/oauth/applications)
2. Create a new Personal Access Token
3. Copy both Client ID and Client Secret
4. Configure using either environment variables or direct config

**Use case**: Single-user applications, scripts, server-to-server

**Stability**: Token never expires (unless revoked)

### OAuth 2.0

```typescript
{
  type: 'oauth';
  accessToken: string;           // REQUIRED
  refreshToken: string;          // REQUIRED
  onRefresh: (tokens: {          // REQUIRED
    accessToken: string;
    refreshToken: string;
  }) => void | Promise<void>;
  onRefreshFailure: (error: Error) => void | Promise<void>;  // REQUIRED
  clientId?: string;             // Optional (can use PCO_APP_ID env var)
  clientSecret?: string;          // Optional (can use PCO_APP_SECRET env var)
}
```

**Use case**: Multi-user applications, web apps

**Lifecycle**: Access tokens expire (typically 1 hour), must handle refresh

**Invariant**: `onRefresh` and `onRefreshFailure` are **required** callbacks

### Basic Auth

```typescript
{
  type: 'basic';
  appId: string;                 // REQUIRED
  appSecret: string;             // REQUIRED
}
```

**Use case**: Server-to-server integrations

## Base URL

```typescript
{
  baseURL?: string;  // Default: 'https://api.planningcenteronline.com/people/v2'
}
```

Override the base URL for the API (useful for testing or proxies).

## Timeout

```typescript
{
  timeout?: number;  // Default: 30000 (30 seconds, milliseconds)
}
```

Request timeout in milliseconds.

## Headers

```typescript
{
  headers?: Record<string, string>;
}
```

Custom headers to include with every request.

**Example**:

```typescript
{
  headers: {
    'X-Custom-Header': 'value',
    'User-Agent': 'MyApp/1.0'
  }
}
```

## Debug

```typescript
{
  debug?: boolean | {
    prefix?: string;
    includePayloads?: boolean;
    onLog?: (message: string, data?: object) => void;
  };
}
```

When set, the client logs each request (start, complete, error) to the console or your custom `onLog` sink. Use `debug: true` for simple logging, or an object for a prefix, optional payload logging, and a custom log function.

## Environment Variables

The library respects these environment variables:

- `PCO_APP_ID`: OAuth client ID (used if not provided in config)
- `PCO_APP_SECRET`: OAuth client secret (used if not provided in config)

**Usage**:

```typescript
// Can omit from config if env vars are set
{
  type: 'oauth',
  accessToken: 'token',
  refreshToken: 'refresh',
  onRefresh: async (tokens) => { /* ... */ },
  onRefreshFailure: async (error) => { /* ... */ }
  // clientId and clientSecret will use PCO_APP_ID and PCO_APP_SECRET
}
```

## Configuration Precedence

1. **Config object** - Highest precedence
2. **Environment variables** - Falls back if not in config
3. **Defaults** - Used if neither config nor env vars provided

## Complete Example

```typescript
const client = new PcoClient({
  auth: {
    type: 'personal_access_token',
    personalAccessToken: process.env.PCO_PERSONAL_ACCESS_TOKEN!
  },
  baseURL: 'https://api.planningcenteronline.com/people/v2',
  timeout: 30000,
  headers: { 'User-Agent': 'MyApp/1.0' },
  debug: true  // optional: request logging
});
```

## Type Reference

See [API Documentation](/docs/api) or package `index.ts` for full types:

- `PcoClientConfig`
- `PcoAuthConfig`
- `PcoDebugOptions`
- `PersonalAccessTokenAuth`
- `OAuthAuth`
- `BasicAuth`

## Validation

- **Auth**: Required; must be one of the three auth types
- **OAuth**: `onRefresh` and `onRefreshFailure` are required
- **Timeout**: Must be a positive number when provided

Invalid configuration throws an error during client creation.

