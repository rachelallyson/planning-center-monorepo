# PCO API vertices (combined)

Static snapshot of People and Check-Ins vertex docs. Generated directly here by:

- `npm run docs:export-people-site` → `people/`
- `npm run docs:export-check-ins-site` → `check-ins/`
- `npm run docs:export-vertices` → runs both exports and writes `index.html`

When running the docs app (`npm run docs:dev`), open http://localhost:3333/vertices/
