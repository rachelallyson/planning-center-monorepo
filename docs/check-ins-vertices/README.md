# Check-Ins API vertices

Output is now generated under **docs/public/vertices/check-ins/** (combined docs site).

From repo root: `npm run docs:export-check-ins-site` (writes to `docs/public/vertices/check-ins/`).  
To view: run `npm run docs:dev` and open http://localhost:3333/vertices/check-ins/
